<Window x:Class="BookOrg.Src.UI.DBInteraction.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:BookOrg.Src.UI.DBInteraction"
        mc:Ignorable="d"
        Title="BookOrg - Library Manager"
        WindowState="Maximized"
        ResizeMode="CanResize">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Border Grid.Row="0" Background="#f0f0f0" BorderBrush="#cccccc" BorderThickness="0,0,0,1" 
                Padding="10" Visibility="{Binding BackButtonVisibility}">
            <StackPanel Orientation="Horizontal">
                <Button Content="Back to Menu" Click="BackToMenuClick" 
                        Padding="15,5" FontSize="14" FontWeight="Bold"/>
            </StackPanel>
        </Border>

        <ContentControl Grid.Row="1" Content="{Binding CurrentControl}" />
    </Grid>
</Window>