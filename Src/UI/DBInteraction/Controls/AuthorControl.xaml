<local:EntityControlBase x:Class="BookOrg.Src.UI.DBInteraction.Controls.AuthorControl"
             x:TypeArguments="entities:Author"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:BookOrg.Src.UI.DBInteraction.Controls"
             xmlns:entities="clr-namespace:BookOrg.Src.Logic.Core.DBEntities"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid Background="LightGreen">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Orientation="Horizontal" Margin="10" Grid.Row="0" VerticalAlignment="Center">
            <TextBlock Text="Authors" FontSize="25" FontWeight="Bold" VerticalAlignment="Center"/>
            <Button Click="AddNewAuthorClick" Style="{StaticResource AddButtonStyle}"/>
        </StackPanel>

        <DataGrid ItemsSource="{Binding Items}" AutoGenerateColumns="False" CanUserAddRows="False"
                  HeadersVisibility="Column" GridLinesVisibility="Horizontal" CellEditEnding="DataGridCellEditEnding"
                  Background="White" RowHeaderWidth="0" Grid.Row="1" FontSize="20">

            <DataGrid.Columns>
                <DataGridTextColumn Header="Author Name" Binding="{Binding AuthorName, UpdateSourceTrigger=PropertyChanged}" Width="*"/>

                <DataGridTemplateColumn Width="90">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <Button Click="DeleteAuthorClick" Style="{StaticResource RemoveButtonStyle}"/>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</local:EntityControlBase>