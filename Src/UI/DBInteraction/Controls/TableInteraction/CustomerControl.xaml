<local1:EntityControlBase x:Class="BookOrg.Src.UI.DBInteraction.Controls.TableInteraction.CustomerControl"
                         x:TypeArguments="entities:Customer"
                         xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                         xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                         xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
                         xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
                         xmlns:local1="clr-namespace:BookOrg.Src.UI.DBInteraction.Controls.TableInteraction"
                         xmlns:entities="clr-namespace:BookOrg.Src.Logic.Core.DBEntities"
                         mc:Ignorable="d" 
                         d:DesignHeight="450" d:DesignWidth="800">
    <Grid Background="#ffffa1">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Orientation="Horizontal" Margin="10" Grid.Row="0" VerticalAlignment="Center">
            <TextBlock Text="Customers" FontSize="25" FontWeight="Bold" VerticalAlignment="Center"/>
            <Button Click="AddNewCustomerClick" Style="{StaticResource AddButtonStyle}"/>
        </StackPanel>

        <DataGrid ItemsSource="{Binding Items}" AutoGenerateColumns="False" CanUserAddRows="False"
                  HeadersVisibility="Column" GridLinesVisibility="Horizontal" CellEditEnding="DataGridCellEditEnding"
                  Background="White" RowHeaderWidth="0" Grid.Row="1" FontSize="20">

            <DataGrid.Columns>
                <DataGridTextColumn Header="First Name" Binding="{Binding FirstName, UpdateSourceTrigger=PropertyChanged}" Width="*"/>
                <DataGridTextColumn Header="Last Name" Binding="{Binding LastName, UpdateSourceTrigger=PropertyChanged}" Width="*"/>
                <DataGridTextColumn Header="Email" Binding="{Binding Email, UpdateSourceTrigger=PropertyChanged}" Width="*"/>

                <DataGridTemplateColumn Width="90">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <Button Click="DeleteCustomerClick" Style="{StaticResource RemoveButtonStyle}"/>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</local1:EntityControlBase>
